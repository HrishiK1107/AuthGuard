export default function ActiveBlocks() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-semibold">Active Blocks & Rate Limits</h1>
      <p className="text-sm text-gray-400 mt-2">
        Currently blocked IPs, users, and enforced rate limits.
      </p>
    </div>
  )
}
